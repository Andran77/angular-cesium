import { MapCesiumDirective } from './map-cesium.directive';

describe('MapCesiumDirective', () => {
  it('should create an instance', () => {
    const directive = new MapCesiumDirective();
    expect(directive).toBeTruthy();
  });
});
